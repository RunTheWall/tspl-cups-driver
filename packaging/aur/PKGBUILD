# Maintainer: Run The Wall <hello@constly.com>
#
# Do not hand-edit pkgver, sha256sums or .SRCINFO: they are derived from the
# released tarball by  sh packaging/aur/refresh.sh <version>,  which the
# release workflow runs before publishing to the AUR. Editing one of the three
# without the others is what breaks AUR installs (checksum mismatch, or stale
# metadata that installs the wrong version).
pkgname=tspl-cups-driver
pkgver=1.3.5
pkgrel=1
pkgdesc="Free CUPS driver for TSPL/TSPL2 thermal label printers (by Run The Wall - https://constly.com)"
arch=('x86_64' 'aarch64' 'armv7h')
url="https://github.com/RunTheWall/tspl-cups-driver"
license=('MIT')
depends=('cups')
conflicts=('hzd950-cups-driver')
replaces=('hzd950-cups-driver')
provides=('hzd950-cups-driver')
source=("$pkgname-$pkgver.tar.gz::$url/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('4cff46344b1889d8d4a5bf83217021e7b2911b5b2147f948085ef0f666d0bdab')

build() {
  cd "$pkgname-$pkgver"
  make
}

check() {
  cd "$pkgname-$pkgver"
  # Hardware-free: synthetic raster in, exact TSPL bytes asserted. Catches a
  # release whose filter output moved without anyone noticing.
  make test
}

package() {
  cd "$pkgname-$pkgver"
  local sb; sb="$(cups-config --serverbin)"
  install -Dm0755 src/rastertotspl    "$pkgdir$sb/filter/rastertotspl"
  install -Dm0700 backend/tspl     "$pkgdir$sb/backend/tspl"
  install -Dm0644 ppd/tspl-label.ppd "$pkgdir/usr/share/ppd/tspl/tspl-label.ppd"
  install -Dm0644 LICENSE            "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
  install -Dm0644 README.md          "$pkgdir/usr/share/doc/$pkgname/README.md"
}
