# Stable /dev symlink for TSPL label printers, so the device path survives
# reboot USB re-enumeration when you have more than one USB printer.
# Covers the same USB ids the tspl backend auto-detects.
# Part of the free RTW driver — https://constly.com
#
# Install:  sudo cp 99-tspl-label.rules /etc/udev/rules.d/
#           sudo udevadm control --reload-rules && sudo udevadm trigger
#
# 0fe6:811e  HZD950-PRO / HERO (tested)
# 09c6:0426  Beeprt/JADENS/Munbyn-ITPP941 shared OEM engine
# 20d1:7008  iDPRT SP410/SP420 (HPRT family)
SUBSYSTEM=="usbmisc", KERNEL=="lp[0-9]*", ATTRS{idVendor}=="0fe6", ATTRS{idProduct}=="811e", SYMLINK+="usb/tspl-label"
SUBSYSTEM=="usbmisc", KERNEL=="lp[0-9]*", ATTRS{idVendor}=="09c6", ATTRS{idProduct}=="0426", SYMLINK+="usb/tspl-label"
SUBSYSTEM=="usbmisc", KERNEL=="lp[0-9]*", ATTRS{idVendor}=="20d1", ATTRS{idProduct}=="7008", SYMLINK+="usb/tspl-label"
